import 'package:flutter/material.dart';

class Option2 extends StatelessWidget {
  const Option2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Option-2")),
    );
  }
}
